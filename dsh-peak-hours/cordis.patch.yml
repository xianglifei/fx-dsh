# dsh-peak-hours —— DSH bundle patch（挂载声明）
# DSH 启动时按 bundle 层栈叠加各插件 patch，本文件把本插件插入 Web profile 的配置树。
#
# 本地开发安装：
#   dsh plugin --profile web add link:<本目录绝对路径>
- insert:
    - id: dsh-peak-hours
      name: dsh-peak-hours
